# Sync

19 `SystemSyncProcessor` instances registered in `systemModuleInitializerProvider`, one per entity type. Remote table names match Supabase migration `20250712000015_system_security_enterprise.sql`.
