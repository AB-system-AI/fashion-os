# Testing Strategy

```bash
flutter test test/features/sales/
```

- `sales_order_engine_test.dart` — calculations, transitions, reservations
- `sales_sync_processor_test.dart` — entity/table mapping
- `sales_permissions_test.dart` — RBAC codes
